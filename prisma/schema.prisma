generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model File {
  id        String   @id @default(cuid())
  input     String
  aiResult  Json
  source    String
  createdAt DateTime @default(now())
}


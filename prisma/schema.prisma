generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model File {
  id        String   @id @default(uuid())
  name      String
  size      Int
  aiResult  Json
  createdAt DateTime @default(now())
}
